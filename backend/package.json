{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "build": "node -e \"console.log('🔨 Building Backend...')\" && tsc",
    "dev": "node -e \"console.log('🔧 Starting Backend Development Server...')\" && nodemon src/server.ts",
    "db:generate": "node -e \"console.log('⚙️ Generating Prisma Client...')\" && prisma generate",
    "db:push": "node -e \"console.log('📤 Pushing database schema...')\" && prisma db push",
    "db:migrate": "node -e \"console.log('🔄 Running database migrations...')\" && prisma migrate dev",
    "db:studio": "node -e \"console.log('🎨 Opening Prisma Studio...')\" && prisma studio",
    "db:reset": "node -e \"console.log('⚠️ Resetting database...')\" && prisma migrate reset",
    "db:seed": "node -e \"console.log('🌱 Seeding database...')\" && ts-node scripts/seedDatabase.ts",
    "db:seed:admin": "node -e \"console.log('👑 Seeding Super Admin...')\" && ts-node scripts/seedSuperAdmin.ts",
    "db:seed:institution": "node -e \"console.log('🌱 Seeding institution data...')\" && ts-node scripts/seedInstitutionData.ts",
    "docker:logs": "node -e \"console.log('📋 Showing Docker logs...')\" && docker-compose logs -f postgres-dev",
    "docker:ps": "node -e \"console.log('📊 Docker container status...')\" && docker-compose ps",
    "docker:dev": "node -e \"console.log('🐳 Starting Docker Dev Environment...')\" && docker-compose up postgres-dev",
    "docker:prod": "node -e \"console.log('🐳 Starting Docker Production Environment...')\" && docker-compose --profile prod up postgres-prod",
    "docker:dev:build": "node -e \"console.log('🐳 Building and Starting Docker Dev Environment...')\" && docker-compose up --build postgres-dev",
    "docker:prod:build": "node -e \"console.log('🐳 Building and Starting Docker Production Environment...')\" && docker-compose --profile prod up --build postgres-prod",
    "docker:down": "node -e \"console.log('🛑 Stopping Docker containers...')\" && docker-compose down",
    "docker:down:volumes": "node -e \"console.log('🛑 Stopping Docker containers and removing volumes...')\" && docker-compose down -v",
    "test": "node -e \"console.log('🧪 Running Backend Tests...')\" && node -e \"console.log('⚠️ No tests configured yet')\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "@types/node-cron": "^3.0.11",
    "@types/socket.io": "^3.0.1",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "exceljs": "^4.4.0",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/morgan": "^1.9.10",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.3.0",
    "nodemon": "^3.1.10",
    "prisma": "^6.19.0",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.9.2"
  }
}
